package sakayukari

import "testing"

func TestParserError(t *testing.T) {
	// only testing succeeds (not failures) for simplicity
	/*
		var cases = []string{
			"a := b",
			"object message",
			`Log debug: "some message"`,
			`with (Log name: "sensor-component")`,
		}
	*/
}
